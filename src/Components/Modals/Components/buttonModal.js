export default function ButtonModal({labelButton, className, onClick}){
    return (
        <button 
        className="bg-red-500 rounded p-2 w-[150px] text-white text-[13pt] font" > labelButton
        </button>
    )
}